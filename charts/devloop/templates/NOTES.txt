devloop chart deployed successfully.

Components:
{{- if .Values.temporalWorker.enabled }}
  - Temporal Orchestration Worker: {{ .Release.Name }}-temporal-worker
{{- end }}
{{- if .Values.temporal.enabled }}
  - Bundled Temporal (evaluation): frontend at {{ include "devloop.temporalHost" . }}

NOTE: The bundled Temporal subchart is intended for evaluation. For
production, deploy Temporal separately with real persistence and set
`temporalHost` (see docs/temporal-prerequisites.md).
{{- else }}

NOTE: Temporal must already be running at {{ include "devloop.temporalHost" . }}.
This chart does NOT deploy Temporal unless you set temporal.enabled=true.
{{- end }}
